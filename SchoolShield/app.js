const $ = s => document.querySelector(s), $$ = s => [...document.querySelectorAll(s)];
const ROLE_NAMES = { principal: 'Principal', deputy: 'Deputy Principal', sgb: 'SGB Member', security: 'Security Officer', parent: 'Parent / Guardian', clerk: 'School Clerk', teacher: 'Teacher' };
const ACCESS = {
    principal: ['dashboard', 'leadership', 'visitors', 'incidents', 'notifications', 'learners', 'parents', 'staff', 'security', 'reports', 'settings', 'announcements', 'sick-notices', 'appointments', 'student-records', 'student-record', 'student-reports', 'test-scores', 'teacher-chat', 'parent-chat'],
    deputy: ['dashboard', 'leadership', 'visitors', 'incidents', 'notifications', 'learners', 'parents', 'staff', 'security', 'reports', 'settings', 'announcements', 'sick-notices', 'appointments', 'student-records', 'student-record', 'student-reports', 'test-scores', 'teacher-chat', 'parent-chat'],
    sgb: ['dashboard', 'incidents', 'notifications', 'reports'],
    security: ['dashboard', 'visitors', 'incidents', 'notifications', 'security-officers', 'reports'],
    parent: ['dashboard', 'sick-notice', 'notifications', 'student-record', 'teacher-chat', 'incidents'],
    clerk: ['dashboard', 'student-records', 'announcements', 'sick-notices', 'appointments'],
    teacher: ['dashboard', 'learners', 'announcements', 'sick-notices', 'student-reports', 'test-scores', 'incidents', 'parent-chat']
};
const NAV = [
    ['dashboard', 'Dashboard', 'dashboard.html', 'grid'], ['leadership', 'Leadership', 'leadership.html', 'lead'], ['visitors', 'Visitors', 'visitors.html', 'users'], ['incidents', 'Incidents', 'incidents.html', 'alert'], ['notifications', 'Notifications', 'notifications.html', 'bell'], ['learners', 'Learners', 'learners.html', 'student'], ['parents', 'Parents', 'parents.html', 'parent'], ['staff', 'Staff', 'staff.html', 'staff'], ['security', 'Security Officers', 'security.html', 'shield'], ['reports', 'Reports', 'reports.html', 'report'], ['settings', 'Settings', 'settings.html', 'settings'],
    ['announcements', 'Announcements', 'announcements.html', 'megaphone'], ['sick-notices', 'Sick Notices', 'sick-notices.html', 'medical'], ['appointments', 'Appointments', 'appointments.html', 'calendar'], ['student-records', 'Student Records', 'student-records.html', 'folder'], ['student-record', 'Student Record', 'student-record.html', 'record'], ['student-reports', 'Student Reports', 'student-reports.html', 'report'], ['test-scores', 'Test Scores', 'test-scores.html', 'scores'], ['sick-notice', 'Sick Notice', 'sick-notice.html', 'medical'], ['teacher-chat', 'Teacher Chat', 'teacher-chat.html', 'chat'], ['parent-chat', 'Parent–Teacher Chat', 'parent-chat.html', 'chat'], ['security-officers', 'Security Officers', 'security-officers.html', 'shield']
];
const ICON = { grid: '▦', lead: '♙', users: '♙', alert: '⚠', bell: '◈', student: '♙', parent: '○', staff: '♟', shield: '✓', report: '▤', settings: '⚙', megaphone: '◉', medical: '✚', calendar: '▣', folder: '▰', record: '▤', scores: '▥', chat: '◌' };
const seed = {
    visitors: [{ id: 'VIS-1042', name: 'Thabo Molefe', host: 'Ms Mokoena', purpose: 'Parent meeting', in: '08:12', out: '—', status: 'Inside', type: 'Parent' }, { id: 'VIS-1041', name: 'Lerato Dlamini', host: 'Mr Naidoo', purpose: 'Delivery', in: '08:05', out: '09:03', status: 'Checked Out', type: 'Service Provider' }, { id: 'VIS-1040', name: 'Mpho Khumalo', host: 'Mrs Jacobs', purpose: 'Appointment', in: '07:54', out: '—', status: 'Inside', type: 'Visitor' }, { id: 'VIS-1039', name: 'Sipho Ndlovu', host: 'Admin Office', purpose: 'Document collection', in: '07:40', out: '08:25', status: 'Checked Out', type: 'Visitor' }],
    incidents: [{ id: 'INC-2026-018', date: '10 Sep 2026', time: '10:18', category: 'Safety', location: 'Main Gate', priority: 'High', status: 'Open', officer: 'S Ndlovu', description: 'Unidentified vehicle stopped at the entrance.' }, { id: 'INC-2026-017', date: '10 Sep 2026', time: '09:42', category: 'Medical', location: 'Grade 5 Block', priority: 'Medium', status: 'Under Investigation', officer: 'T Mokoena', description: 'Learner reported feeling unwell.' }, { id: 'INC-2026-016', date: '09 Sep 2026', time: '14:20', category: 'Security', location: 'Sports Field', priority: 'Critical', status: 'Resolved', officer: 'P Dube', description: 'Gate access alarm triggered.' }],
    notifications: [{ id: 'NTF-8841', time: '10:18', category: 'Emergency', priority: 'Critical', title: 'Security alert', description: 'Unidentified vehicle stopped at the entrance.', read: false }, { id: 'NTF-8840', time: '09:42', category: 'Incident', priority: 'High', title: 'Medical incident reported', description: 'Assistance requested at Grade 5 Block.', read: false }, { id: 'NTF-8839', time: '08:55', category: 'Visitor', priority: 'Medium', title: 'Visitor overstay', description: 'VIS-1042 has remained on campus for an extended period.', read: true }],
    learners: [{ id: 'STU-1001', name: 'Amogelang Mokoena', grade: 'Grade 5', class: '5A', parent: 'Thabo Molefe', status: 'Active' }, { id: 'STU-1002', name: 'Lethabo Dube', grade: 'Grade 5', class: '5A', parent: 'Naledi Dube', status: 'Active' }, { id: 'STU-1003', name: 'Karabo Ndlovu', grade: 'Grade 8', class: '8B', parent: 'Sipho Ndlovu', status: 'Active' }, { id: 'STU-1004', name: 'Neo Maseko', grade: 'Grade 10', class: '10C', parent: 'Mpho Maseko', status: 'Active' }],
    staff: [{ id: 'EMP-001', name: 'Mpho Mokoena', role: 'Teacher', department: 'Grade 5', status: 'Active' }, { id: 'EMP-002', name: 'Thandi Dube', role: 'Teacher', department: 'Grade 8', status: 'Active' }, { id: 'EMP-003', name: 'Sello Ndlovu', role: 'Security Officer', department: 'Security', status: 'Active' }, { id: 'EMP-004', name: 'Lerato Jacobs', role: 'Clerk', department: 'Administration', status: 'Active' }],
    security: [{ id: 'SEC-001', name: 'Sello Ndlovu', employee: 'EMP-031', shift: '06:00–14:00', site: 'Main Gate', status: 'On Duty' }, { id: 'SEC-002', name: 'Thato Mokoena', employee: 'EMP-032', shift: '14:00–22:00', site: 'Main Gate', status: 'Scheduled' }, { id: 'SEC-003', name: 'Palesa Dube', employee: 'EMP-033', shift: '06:00–14:00', site: 'Sports Field', status: 'On Duty' }]
};
function getState() { let s = JSON.parse(localStorage.getItem('schoolshield') || '{}'); for (const [k, v] of Object.entries(seed)) if (!s[k]) s[k] = v; return s }
function save(s) { localStorage.setItem('schoolshield', JSON.stringify(s)) }
function role() { return sessionStorage.getItem('schoolshieldRole') || 'principal' }
function page() { return document.body.dataset.page || 'dashboard' }
function prefix() { return './' }
function allowed(p = page()) { return (ACCESS[role()] || []).includes(p) }
function go(url) { location.href = prefix() + url }
function icon(k) { return `<span class="icon">${ICON[k] || '•'}</span>` }
function badge(v) { const c = ['Critical', 'High', 'Open'].includes(v) ? 'danger' : ['Medium', 'Under Investigation', 'Scheduled'].includes(v) ? 'warn' : ['Resolved', 'Inside', 'Active', 'On Duty', 'Read'].includes(v) ? 'success' : 'neutral'; return `<span class="badge ${c}">${v}</span>` }
function nav() { const r = role(); return NAV.filter(n => ACCESS[r]?.includes(n[0])).map(([id, label, url, ic]) => `<a href="${url}" class="nav-item ${page() === id ? 'active' : ''}">${icon(ic)}<span>${label}</span></a>`).join('') }
function shell(body, title) { const r = role(); return `<div class="app-shell"><aside class="sidebar"><div class="brand"><img src="assets/schoolshield-logo.png"><div><strong>SchoolShield</strong><small>School safety platform</small></div></div><div class="menu-title">Workspace</div><nav>${nav()}</nav><div class="sidebar-bottom"><div class="role-card"><div class="avatar">${ROLE_NAMES[r].slice(0, 2).toUpperCase()}</div><div><b>${ROLE_NAMES[r]}</b><small>${r === 'sgb' ? 'View-only access' : r === 'principal' || r === 'deputy' ? 'Full access' : 'Role-based access'}</small></div></div><button class="logout" id="logout">↪ <span>Sign out</span></button></div></aside><main class="main"><header class="topbar"><div><div class="eyebrow">SchoolShield</div><h2>${title}</h2></div><div class="top-actions"><button class="icon-btn" id="globalBell" title="Notifications">◈</button><div class="profile"><div class="avatar">${ROLE_NAMES[r].slice(0, 2).toUpperCase()}</div><div><b>${ROLE_NAMES[r]}</b><small>Riverside Secondary School</small></div></div></div></header><section class="content">${body}</section></main></div>` }
function stat(label, value, sub, trend = '') { return `<div class="stat-card"><div class="stat-icon">▦</div><div><div class="stat-value">${value}</div><div class="stat-label">${label}</div><small>${trend || sub}</small></div></div>` }
function table(headers, rows, empty = 'No records found') { return `<div class="table-wrap"><table><thead><tr>${headers.map(h => `<th>${h}</th>`).join('')}</tr></thead><tbody>${rows.length ? rows.join('') : `<tr><td colspan="${headers.length}" class="empty">${empty}</td></tr>`}</tbody></table></div>` }
function searchBar(id, placeholder = 'Search...') { return `<div class="toolbar"><div class="search"><span>⌕</span><input id="${id}" placeholder="${placeholder}"></div><button class="btn ghost" data-action="filter">Filters</button></div>` }
function dashboard() { const s = getState(), r = role(); const accessLabel = r === 'sgb' ? 'Governance overview' : r === 'security' ? 'Security operations' : r === 'parent' ? 'Your learner & school updates' : r === 'teacher' ? 'Your class workspace' : r === 'clerk' ? 'Administration workspace' : 'Campus overview'; return shell(`<div class="hero"><div><span class="pill">${accessLabel}</span><h1>Good morning, ${ROLE_NAMES[r]}</h1><p>One secure workspace for school safety, people, incidents and communication.</p></div><div class="hero-actions">${r !== 'sgb' ? `<button class="btn primary" data-action="incident">+ Report incident</button>` : ''}<button class="btn ghost" data-nav="notifications">View alerts</button></div></div><div class="stats-grid">${stat('Visitors today', s.visitors.length, 'Registered visits')}${stat('On campus', s.visitors.filter(v => v.status === 'Inside').length, 'Currently inside')}${stat('Open incidents', s.incidents.filter(i => i.status !== 'Resolved').length, 'Needs attention')}${stat('Learners', '1,246', 'Enrolled this year')}</div><div class="dashboard-grid"><section class="panel"><div class="panel-head"><div><h3>Safety overview</h3><p>Latest operational activity</p></div><a class="text-link" href="reports.html">Open reports →</a></div><div class="overview-list"><div><span class="dot danger"></span><div><b>Critical incident</b><small>${s.incidents.find(i => i.priority === 'Critical')?.description || 'No critical incidents'}</small></div><strong>1</strong></div><div><span class="dot warn"></span><div><b>Visitors currently inside</b><small>Gate and reception registrations</small></div><strong>${s.visitors.filter(v => v.status === 'Inside').length}</strong></div><div><span class="dot success"></span><div><b>Resolved incidents</b><small>Closed after review</small></div><strong>${s.incidents.filter(i => i.status === 'Resolved').length}</strong></div></div></section><section class="panel"><div class="panel-head"><div><h3>Recent incidents</h3><p>Most recent reports</p></div><a class="text-link" href="incidents.html">View all →</a></div>${table(['Incident', 'Location', 'Priority', 'Status'], s.incidents.slice(0, 4).map(i => `<tr><td><b>${i.id}</b><small>${i.time}</small></td><td>${i.location}</td><td>${badge(i.priority)}</td><td>${badge(i.status)}</td></tr>`))}</section></div><section class="panel"><div class="panel-head"><div><h3>Quick access</h3><p>Only tools available to your role are shown.</p></div></div><div class="quick-grid">${quickForRole(r)}</div></section>`, 'Dashboard') }
function quickForRole(r) { const q = { principal: [['Register visitor', 'visitors.html'], ['Manage learners', 'learners.html'], ['Review incidents', 'incidents.html'], ['Security deployment', 'security.html'], ['Reports', 'reports.html']], deputy: [['Register visitor', 'visitors.html'], ['Review incidents', 'incidents.html'], ['Manage learners', 'learners.html'], ['Reports', 'reports.html']], sgb: [['Review incidents', 'incidents.html'], ['Open reports', 'reports.html'], ['Read notifications', 'notifications.html']], security: [['Register visitor', 'visitors.html'], ['Report incident', 'incidents.html'], ['Security officers', 'security-officers.html'], ['Alerts', 'notifications.html']], parent: [['Submit sick notice', 'sick-notice.html'], ['Student record', 'student-record.html'], ['Teacher chat', 'teacher-chat.html'], ['School notifications', 'notifications.html']], clerk: [['Student records', 'student-records.html'], ['Announcements', 'announcements.html'], ['Sick notices', 'sick-notices.html'], ['Appointments', 'appointments.html']], teacher: [['My learners', 'learners.html'], ['Student reports', 'student-reports.html'], ['Test scores', 'test-scores.html'], ['Report incident', 'incidents.html']] }[r] || []; return q.map(x => `<a class="quick" href="${x[1]}"><span>${x[0]}</span><b>→</b></a>`).join('') }
function incidents() { const s = getState(); return shell(`<div class="page-intro"><div><span class="pill">Safety management</span><h1>Incidents</h1><p>Record, investigate, escalate and close school safety incidents.</p></div><div>${role() !== 'sgb' ? `<button class="btn primary" data-action="incident">+ Report incident</button>` : ''}</div></div>${searchBar('incidentSearch', 'Search incident ID, location or description')}<section class="panel">${table(['Incident', 'Date & time', 'Category', 'Location', 'Priority', 'Status', 'Officer'], s.incidents.map(i => `<tr><td><b>${i.id}</b></td><td>${i.date}<small>${i.time}</small></td><td>${i.category}</td><td>${i.location}</td><td>${badge(i.priority)}</td><td>${badge(i.status)}</td><td>${i.officer}</td></tr>`))}</section>`, 'Incidents') }
function visitors() { const s = getState(); return shell(`<div class="page-intro"><div><span class="pill">Front office & gate</span><h1>Visitors</h1><p>Register visitors, track campus presence and complete check-outs.</p></div><div class="action-row"><button class="btn ghost" data-action="scan">Scan QR</button><button class="btn primary" data-action="register">+ Register visitor</button></div></div><div class="stats-grid compact">${stat('Inside', s.visitors.filter(v => v.status === 'Inside').length, 'Currently on campus')}${stat('Visits today', s.visitors.length, 'All registrations')}${stat('Checked out', s.visitors.filter(v => v.status === 'Checked Out').length, 'Completed visits')}</div>${searchBar('visitorSearch', 'Search visitor, ID, host or purpose')}<section class="panel">${table(['Visitor', 'Type', 'Host', 'Purpose', 'Check-in', 'Status', ''], s.visitors.map(v => `<tr><td><b>${v.name}</b><small>${v.id}</small></td><td>${v.type}</td><td>${v.host}</td><td>${v.purpose}</td><td>${v.in}</td><td>${badge(v.status)}</td><td><button class="table-action" data-visitor="${v.id}">View</button></td></tr>`))}</section>`, 'Visitors') }
function notifications() { const s = getState(); return shell(`<div class="page-intro"><div><span class="pill">Alerts & communication</span><h1>Notifications</h1><p>Emergency alerts, incident notifications and school updates.</p></div><button class="btn ghost" id="markRead">Mark all read</button></div><div class="notice-stack">${s.notifications.map(n => `<article class="notice ${n.read ? 'read' : ''}"><div class="notice-icon">${n.priority === 'Critical' ? '!' : '◈'}</div><div class="grow"><div class="notice-top"><b>${n.title}</b>${badge(n.priority)}</div><p>${n.description}</p><small>${n.category} · ${n.time} · ${n.read ? 'Read' : 'Unread'}</small></div></article>`).join('')}</div>`, 'Notifications') }
function leadership() { return shell(`<div class="page-intro"><div><span class="pill">Executive oversight</span><h1>Leadership</h1><p>School-wide safety posture and accountability for Principal and Deputy Principal.</p></div></div><div class="stats-grid">${stat('Open incidents', '2', 'Requiring action')}${stat('Critical alerts', '1', 'Active escalation')}${stat('Visitors inside', '3', 'Live campus presence')}${stat('Security officers', '6', 'Assigned workforce')}</div><div class="dashboard-grid"><section class="panel"><div class="panel-head"><div><h3>Leadership priorities</h3><p>Items requiring executive attention</p></div></div><div class="priority-list"><div><b>Review critical security incident</b><span>Due today · Main Gate</span><button class="btn small">Open case</button></div><div><b>Approve security deployment</b><span>Next shift · 14:00</span><button class="btn small">Review</button></div><div><b>Review unresolved medical incident</b><span>Grade 5 Block · 09:42</span><button class="btn small">Open case</button></div></div></section><section class="panel"><div class="panel-head"><div><h3>Campus controls</h3><p>Current operational state</p></div></div><div class="control-list"><div><span>Gate monitoring</span>${badge('Active')}</div><div><span>Visitor registration</span>${badge('Active')}</div><div><span>Emergency notification</span>${badge('Active')}</div><div><span>Incident escalation</span>${badge('Active')}</div></div></section></div>`, 'Leadership') }
function learners() { const s = getState(); return shell(`<div class="page-intro"><div><span class="pill">Learner management</span><h1>${role() === 'teacher' ? 'My Learners' : 'Learners'}</h1><p>${role() === 'teacher' ? 'Learners assigned to your classes and subjects.' : 'Manage learner records, classes and parent links.'}</p></div>${['principal', 'deputy', 'clerk'].includes(role()) ? '<button class="btn primary" data-action="learner">+ Register learner</button>' : ''}</div>${searchBar('learnerSearch', 'Search learner, student ID, class or parent')}<section class="panel">${table(['Learner', 'Grade / class', 'Parent / guardian', 'Status', ''], s.learners.map(l => `<tr><td><b>${l.name}</b><small>${l.id}</small></td><td>${l.grade} · ${l.class}</td><td>${l.parent}</td><td>${badge(l.status)}</td><td><button class="table-action">View record</button></td></tr>`))}</section>`, 'Learners') }
function generic(title, eyebrow, desc, content, actions = '') { return shell(`<div class="page-intro"><div><span class="pill">${eyebrow}</span><h1>${title}</h1><p>${desc}</p></div><div>${actions}</div></div>${content}`, title) }
function staff() { const s = getState(); return generic('Staff', 'People management', 'Manage teaching, administration and support staff.', `<section class="panel">${table(['Employee', 'Role', 'Department', 'Status'], s.staff.map(x => `<tr><td><b>${x.name}</b><small>${x.id}</small></td><td>${x.role}</td><td>${x.department}</td><td>${badge(x.status)}</td></tr>`))}</section>`, '<button class="btn primary" data-action="staff">+ Add staff member</button>') }
function security() { return generic('Security Officers', 'Security operations', 'Manage security personnel, assignments, sites and duty coverage.', `<div class="stats-grid compact">${stat('Officers', '6', 'Active workforce')}${stat('On duty', '3', 'Current shift')}${stat('Sites', '4', 'Protected locations')}${stat('QR checkpoints', '12', 'Registered checkpoints')}</div><section class="panel"><div class="panel-head"><div><h3>Deployment overview</h3><p>Who is covering which site and shift</p></div><a class="text-link" href="security-officers.html">Manage officers →</a></div><div class="deployment"><div><b>Main Gate</b><span>Sello Ndlovu · 06:00–14:00</span>${badge('On Duty')}</div><div><b>Sports Field</b><span>Palesa Dube · 06:00–14:00</span>${badge('On Duty')}</div><div><b>Admin Block</b><span>Unassigned · 14:00–22:00</span>${badge('Open')}</div></div></section>`) }
function reports() { return generic('Reports', 'Insights & accountability', 'Review safety, visitor, security and operational performance.', `<div class="report-grid">${[['Safety incidents', '18', '2 open', 'incidents.html'], ['Visitor activity', '64', '3 currently inside', 'visitors.html'], ['Security coverage', '94%', '1 open deployment', 'security.html'], ['Learner safety', '7', '2 active cases', 'learners.html']].map(x => `<a class="report-card" href="${x[3]}"><span>${x[0]}</span><strong>${x[1]}</strong><small>${x[2]}</small><b>Open report →</b></a>`).join('')}</div><section class="panel"><div class="panel-head"><div><h3>Reporting centre</h3><p>Download or review operational summaries.</p></div><button class="btn primary" data-export>Export CSV</button></div><div class="report-row"><span>Monthly safety report</span><button class="table-action">Preview</button></div><div class="report-row"><span>Visitor register</span><button class="table-action">Preview</button></div><div class="report-row"><span>Security deployment report</span><button class="table-action">Preview</button></div></section>`) }
function settings() { return generic('Settings', 'Administration', 'Configure school profile, access controls and audit preferences.', `<div class="settings-grid"><section class="panel"><h3>School profile</h3><div class="form-grid"><label>School name<input class="input" value="Riverside Secondary School"></label><label>School code<input class="input" value="RSS-001"></label><label>Principal<input class="input" value="Ms Mokoena"></label><label>Contact email<input class="input" value="admin@riverside.school"></label></div></section><section class="panel"><h3>Security controls</h3><div class="setting-row"><div><b>Require re-login after tab closes</b><small>Session expires when the browser tab is closed.</small></div><input type="checkbox" checked disabled></div><div class="setting-row"><div><b>Login audit logging</b><small>Record role, time and successful/failed login events.</small></div><input type="checkbox" checked></div><div class="setting-row"><div><b>Emergency escalation</b><small>Notify authorised leadership and security users.</small></div><input type="checkbox" checked></div></section></div>`) }
function announcements() { return generic('Announcements', 'Communication', 'Create school-wide or class-targeted announcements.', `<section class="panel"><div class="form-grid"><label>Audience<select class="select"><option>Whole school</option><option>Grade 5</option><option>My learners</option><option>Registered parents</option></select></label><label>Delivery<select class="select"><option>In-app notification</option><option>In-app + SMS</option></select></label><label class="full">Title<input class="input" placeholder="Announcement title"></label><label class="full">Message<textarea class="textarea" placeholder="Write the announcement..."></textarea></label></div><div class="panel-foot"><button class="btn primary">Send announcement</button></div></section><section class="panel"><h3>Recent announcements</h3><div class="report-row"><span><b>Parent meeting schedule</b><small>Whole school · Sent today</small></span>${badge('Read')}</div><div class="report-row"><span><b>Grade 5 safety briefing</b><small>Grade 5 · Sent yesterday</small></span>${badge('Read')}</div></section>`) }
function sickNotices() { return generic('Sick Notices', 'Attendance & welfare', 'Receive, review and track student and staff sick notices.', `<section class="panel">${table(['Submitted by', 'Learner / staff', 'Date', 'Reason', 'Status', 'Action'], [['Thabo Molefe', 'Amogelang Mokoena', '10 Sep 2026', 'Medical appointment', badge('Open'), '<button class="table-action">Review</button>'], ['Mpho Mokoena', 'Mpho Mokoena', '09 Sep 2026', 'Illness', badge('Resolved'), '<button class="table-action">View</button>']].map(r => `<tr>${r.map(x => `<td>${x}</td>`).join('')}</tr>`))}</section>`) }
function sickNotice() { return generic('Sick Notice', 'Parent / guardian', 'Notify the school when your learner is absent for health reasons.', `<section class="panel"><div class="form-grid"><label>Learner<select class="select"><option>Amogelang Mokoena — Grade 5A</option></select></label><label>Date<input class="input" type="date"></label><label>Expected return<input class="input" type="date"></label><label>Reason<select class="select"><option>Illness</option><option>Medical appointment</option><option>Other</option></select></label><label class="full">Additional information<textarea class="textarea" placeholder="Optional details for the school..."></textarea></label></div><div class="panel-foot"><button class="btn primary">Submit sick notice</button></div></section>`) }
function appointments() { return generic('Appointments', 'Administration', 'Schedule and track parent, learner and staff appointments.', `<section class="panel">${table(['Appointment', 'With', 'Date', 'Time', 'Status', ''], [['Parent meeting', 'Ms Mokoena', '11 Sep 2026', '10:00', badge('Scheduled'), '<button class="table-action">Open</button>'], ['SGB safety review', 'Principal', '12 Sep 2026', '14:00', badge('Scheduled'), '<button class="table-action">Open</button>'], ['Learner support', 'Grade 5 Team', '13 Sep 2026', '09:30', badge('Open'), '<button class="table-action">Open</button>']].map(r => `<tr>${r.map(x => `<td>${x}</td>`).join('')}</tr>`))}</section>`, '<button class="btn primary">+ New appointment</button>') }
function studentRecord() { return generic('Student Record', 'Learner profile', 'Access the registered learner record available to the parent account.', `<section class="profile-card"><div class="student-avatar">AM</div><div><h2>Amogelang Mokoena</h2><p>STU-1001 · Grade 5A</p></div><span>${badge('Active')}</span></section><div class="dashboard-grid"><section class="panel"><h3>Student details</h3><div class="detail-grid"><div><small>Grade</small><b>Grade 5</b></div><div><small>Class</small><b>5A</b></div><div><small>Parent</small><b>Thabo Molefe</b></div><div><small>Attendance</small><b>96%</b></div></div></section><section class="panel"><h3>Academic record</h3><div class="report-row"><span>Term 2 report</span><button class="btn small">View PDF</button></div><div class="report-row"><span>Latest assessment</span><b>78%</b></div></section></div>`) }
function studentRecords() { return generic('Student Records', 'Administration', 'Manage learner records, parent relationships and reporting information.', `<section class="panel">${table(['Student', 'Grade', 'Parent', 'Attendance', 'Status', ''], getState().learners.map(l => `<tr><td><b>${l.name}</b><small>${l.id}</small></td><td>${l.grade} · ${l.class}</td><td>${l.parent}</td><td>96%</td><td>${badge(l.status)}</td><td><button class="table-action">Edit</button></td></tr>`))}</section>`, '<button class="btn primary" data-action="learner">+ Add learner</button>') }
function studentReports() { return generic('Student Reports', 'Teacher workspace', 'Prepare and send learner reports to registered parents.', `<section class="panel"><div class="panel-head"><div><h3>Report workflow</h3><p>Draft → review → send to registered parent</p></div><button class="btn primary">+ New report</button></div>${table(['Learner', 'Class', 'Term', 'Status', 'Parent delivery'], [['Amogelang Mokoena', '5A', 'Term 2', badge('Resolved'), badge('Read')], ['Lethabo Dube', '5A', 'Term 2', badge('Open'), '<span class="muted">Not sent</span>'], ['Karabo Ndlovu', '8B', 'Term 2', badge('Under Investigation'), '<span class="muted">Not sent</span>']].map(r => `<tr>${r.map(x => `<td>${x}</td>`).join('')}</tr>`))}</section>`) }
function testScores() { return generic('Test Scores', 'Teacher workspace', 'Add permitted assessment scores for learners under your classes.', `<section class="panel"><div class="form-grid"><label>Class<select class="select"><option>Grade 5A</option><option>Grade 8B</option></select></label><label>Assessment<select class="select"><option>Mathematics — Test 2</option><option>Natural Sciences — Test 1</option></select></label></div><div class="score-list">${getState().learners.slice(0, 4).map((l, i) => `<div><span><b>${l.name}</b><small>${l.id}</small></span><input class="input score" value="${[82, 76, 91, 68][i]}"><span>%</span></div>`).join('')}</div><div class="panel-foot"><button class="btn primary">Save scores</button></div></section>`) }
function chat(title, subtitle) { return generic(title, 'Secure communication', subtitle, `<div class="chat-layout"><section class="panel chat-list"><div class="chat-person active"><div class="avatar">TM</div><div><b>Thabo Molefe</b><small>Parent · Grade 5A</small></div><span class="dot success"></span></div><div class="chat-person"><div class="avatar">ND</div><div><b>Naledi Dube</b><small>Parent · Grade 5A</small></div></div></section><section class="panel chat-window"><div class="chat-head"><b>${role() === 'parent' ? 'Ms Mokoena' : 'Thabo Molefe'}</b><small>Registered SchoolShield contact</small></div><div class="messages"><div class="msg other">Good morning. Can we discuss the latest assessment?</div><div class="msg mine">Yes. I can share the report and arrange a meeting.</div></div><div class="chat-compose"><input class="input" placeholder="Write a secure message..."><button class="btn primary">Send</button></div></section></div>`) }
function securityOfficers() { const s = getState(); return generic('Security Officers', 'Security management', 'Manage officer profiles, employee IDs, role badges, shifts, sites and duties.', `<div class="stats-grid compact">${stat('Officers', s.security.length, 'Displayed here')}${stat('On duty', s.security.filter(x => x.status === 'On Duty').length, 'Current shift')}${stat('Sites', '4', 'Configured')}${stat('Checkpoints', '12', 'QR checkpoints')}</div><section class="panel">${table(['Officer', 'Employee ID', 'Shift', 'Assigned site', 'Status', ''], s.security.map(x => `<tr><td><b>${x.name}</b><small>Badge ${x.id}</small></td><td>${x.employee}</td><td>${x.shift}</td><td>${x.site}</td><td>${badge(x.status)}</td><td><button class="table-action">Manage</button></td></tr>`))}</section><section class="panel"><div class="panel-head"><div><h3>Checkpoint control</h3><p>Named QR checkpoints with site/location validation.</p></div><button class="btn primary">+ Create checkpoint</button></div><div class="checkpoint-grid"><div><b>Main Gate — CP-001</b><small>Radius 75m · Active</small></div><div><b>Admin Block — CP-002</b><small>Radius 50m · Active</small></div><div><b>Sports Field — CP-003</b><small>Radius 100m · Active</small></div></div></section>`) }
function parents() { return generic('Parents', 'Family relationships', 'Manage registered parent and guardian relationships.', `<section class="panel">${table(['Parent / guardian', 'Learner', 'Contact', 'Status'], [['Thabo Molefe', 'Amogelang Mokoena', 'Registered contact', badge('Active')], ['Naledi Dube', 'Lethabo Dube', 'Registered contact', badge('Active')], ['Mpho Maseko', 'Neo Maseko', 'Registered contact', badge('Active')]].map(r => `<tr>${r.map(x => `<td>${x}</td>`).join('')}</tr>`))}</section>`) }
function render() { if (!allowed()) { const d = ACCESS[role()]?.[0] || 'dashboard'; if (page() !== 'dashboard') return go(NAV.find(n => n[0] === d)?.[2] || 'dashboard.html') }; let p = page(), html = ''; if (p === 'dashboard') html = dashboard(); else if (p === 'incidents') html = incidents(); else if (p === 'visitors') html = visitors(); else if (p === 'notifications') html = notifications(); else if (p === 'leadership') html = leadership(); else if (p === 'learners') html = learners(); else if (p === 'staff') html = staff(); else if (p === 'parents') html = parents(); else if (p === 'security') html = security(); else if (p === 'security-officers') html = securityOfficers(); else if (p === 'reports') html = reports(); else if (p === 'settings') html = settings(); else if (p === 'announcements') html = announcements(); else if (p === 'sick-notices') html = sickNotices(); else if (p === 'sick-notice') html = sickNotice(); else if (p === 'appointments') html = appointments(); else if (p === 'student-record') html = studentRecord(); else if (p === 'student-records') html = studentRecords(); else if (p === 'student-reports') html = studentReports(); else if (p === 'test-scores') html = testScores(); else if (p === 'teacher-chat') html = chat('Teacher Chat', 'Communicate securely with your learner’s registered teacher.'); else if (p === 'parent-chat') html = chat('Parent–Teacher Chat', 'Communicate securely with registered parents of your learners.'); else html = dashboard(); $('#app').innerHTML = html; bind() }
function bind() { $('#logout')?.addEventListener('click', () => { sessionStorage.removeItem('schoolshieldRole'); location.href = 'login.html' }); $('#globalBell')?.addEventListener('click', () => go('notifications.html')); $$('[data-nav]').forEach(b => b.onclick = () => { const n = NAV.find(x => x[0] === b.dataset.nav); if (n) go(n[2]) }); $$('[data-action]').forEach(b => b.onclick = () => action(b.dataset.action)); $('#markRead')?.addEventListener('click', () => { let s = getState(); s.notifications.forEach(n => n.read = true); save(s); render() }); $$('[data-visitor]').forEach(b => b.onclick = () => visitorModal(b.dataset.visitor)); $('#visitorSearch')?.addEventListener('input', e => filterTable(e.target.value, '.visitor-row')); $('#incidentSearch')?.addEventListener('input', e => filterTable(e.target.value, '.incident-row')); $('#exportCsv')?.addEventListener('click', exportCsv) }
function filterTable(q, selector) { document.querySelectorAll(selector).forEach(r => r.style.display = r.innerText.toLowerCase().includes(q.toLowerCase()) ? '' : 'none') }
function visitorModal(id) { const v = getState().visitors.find(x => x.id === id); if (!v) return; modal('Visitor details', `<div class="detail-grid"><div><small>Name</small><b>${v.name}</b></div><div><small>Visitor ID</small><b>${v.id}</b></div><div><small>Host</small><b>${v.host}</b></div><div><small>Purpose</small><b>${v.purpose}</b></div><div><small>Check-in</small><b>${v.in}</b></div><div><small>Status</small>${badge(v.status)}</div></div>`) }
function modal(title, body) { const el = document.createElement('div'); el.className = 'modal-backdrop'; el.innerHTML = `<div class="modal"><div class="modal-head"><h3>${title}</h3><button class="close">×</button></div><div class="modal-body">${body}</div></div>`; document.body.appendChild(el); el.querySelector('.close').onclick = () => el.remove(); el.onclick = e => { if (e.target === el) el.remove() } }
function action(type) { if (type === 'register') modal('Register visitor', `<div class="form-grid"><label>Full name<input class="input" placeholder="Visitor full name"></label><label>Visitor type<select class="select"><option>Visitor</option><option>Parent</option><option>Service Provider</option></select></label><label>Person visiting<input class="input" placeholder="Staff member / office"></label><label>Purpose<input class="input" placeholder="Reason for visit"></label></div><div class="modal-foot"><button class="btn primary">Register & check in</button></div>`); else if (type === 'incident') modal('Report incident', `<div class="form-grid"><label>Category<select class="select"><option>Safety</option><option>Medical</option><option>Security</option><option>Behaviour</option></select></label><label>Priority<select class="select"><option>Low</option><option>Medium</option><option>High</option><option>Critical</option></select></label><label>Location<input class="input" placeholder="Where did it happen?"></label><label class="full">Description<textarea class="textarea" placeholder="Describe what happened..."></textarea></label></div><div class="modal-foot"><button class="btn primary">Submit incident</button></div>`); else if (type === 'scan') modal('QR checkpoint / visitor scan', `<div class="scan-box"><div class="qr">⌗</div><b>Ready to scan</b><p>Validate the QR checkpoint and confirm the authorised visitor or guard assignment.</p><input class="input" placeholder="Enter code, e.g. CP-001"></div>`); else modal('Quick action', `<p>This action is available in your role workspace.</p>`) }
function exportCsv() { const s = getState(); const rows = [['ID', 'Time', 'Category', 'Priority', 'Title', 'Description'], ...s.notifications.map(n => [n.id, n.time, n.category, n.priority, n.title, n.description])]; const csv = rows.map(r => r.map(x => '"' + String(x).replaceAll('"', '""') + '"').join(',')).join('\n'); const a = document.createElement('a'); a.href = URL.createObjectURL(new Blob([csv], { type: 'text/csv' })); a.download = 'schoolshield-report.csv'; a.click() }
render();
